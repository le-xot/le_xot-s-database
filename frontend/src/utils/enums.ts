export enum Statuses {
  QUEUE = 'QUEUE',
  DONE = 'DONE',
  PROGRESS = 'PROGRESS',
  UNFINISHED = 'UNFINISHED',
  DROP = 'DROP',
}

export enum Genres {
  ANIME = 'ANIME',
  MOVIE = 'MOVIE',
  CARTOON = 'CARTOON',
  SERIES = 'SERIES',
}

export enum Grades {
  RECOMMEND = 'RECOMMEND',
  LIKE = 'LIKE',
  BEER = 'BEER',
  DISLIKE = 'DISLIKE',
}

export enum Types {
  PAID = 'PAID',
  FREE = 'FREE',
}
